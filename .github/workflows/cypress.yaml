name: Cypress Tests by Priyam

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      
      - name: Edit watcher
        run: |
          npm install
          npm run start:ci:windows
          sudo npm install -g browserstack-cypress-cli
      
      - name: BrowserStack Test
        env:
          BROWSERSTACK_USERNAME: laxmidubey_PYrLpm
          BROWSERSTACK_ACCESS_KEY: 8mq5pvphujadbFMJbJhF
        run : |
          browserstack-cypress run --sync
              
